import { TypeComptePipe } from './type-compte.pipe';

describe('TypeComptePipe', () => {
  it('create an instance', () => {
    const pipe = new TypeComptePipe();
    expect(pipe).toBeTruthy();
  });
});
